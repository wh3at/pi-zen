## Commits

Use Conventional Commits: `<type>[optional scope][!]: <description>` (e.g. `feat: add session export`, `fix(ui): restore focus`).

## Agent skills

### Issue tracker

Issues and specs live in GitHub Issues for `wh3at/pi-zen`. Before issue operations, read `docs/agents/issue-tracker.md`.

### Triage labels

Use the five default triage labels. Before triaging, read `docs/agents/triage-labels.md`.

### Domain docs

This repo uses single-context domain docs. Before exploring the codebase, read `docs/agents/domain.md`.
